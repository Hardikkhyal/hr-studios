# Core Web Framework
Django>=4.2,<5.2

# Image processing for Django ImageField (Product images, etc.)
Pillow>=9.0.0

# Cryptography library for Paytm checksum and AES encryption
pycryptodome>=3.15.0

# Range filter extension for Django Admin
django-admin-rangefilter>=0.10.0
